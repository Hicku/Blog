<H1>Dashboard</H1>


<div>
    {{>newPost}}


    {{#if posts}}
        {{#each posts as |post|}}
        {{> post}}
        {{/each}}
    {{else}}
        <p>"You have no posts"</p>  
    {{/if}}

</div>