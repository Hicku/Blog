<h1>Profile</h1>
<div data-user-id="{{user.id}}">
    <div>{{user.id}}</div>
    <button class="follow-button">Follow</button>
</div>


<div>
    {{#if posts}}
        {{#each posts as |post|}}
        {{> post}}
        {{/each}}
    {{else}}
        <p>"They have no posts"</p>  
    {{/if}}

</div> <dir>