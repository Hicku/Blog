
<div class="header-div" >

</div>
<div class="post-container">
    <div class="main-div-profile">
        <div class="post-div">
            {{#if posts }}
                {{#each posts as |post|}}
                    {{> homepagePost }}
                {{/each}}
            {{else}}
                <p>"They have no posts"</p>
            {{/if}}
        </div>
    </div>
</div>